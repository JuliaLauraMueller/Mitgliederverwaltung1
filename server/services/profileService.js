/*
const db = require('../helpers/db');
const Profile = db.Profile;

module.exports = {
  getById
};

async function getById(id) {
  return await Profile.findById(id).select();
}

// TODO maybe get stuff from other collections
*/
